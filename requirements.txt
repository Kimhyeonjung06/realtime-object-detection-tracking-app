# --- UI ---
# HF Spaces(Python 3.10+)는 gradio 5, 로컬 Python 3.9 환경은 gradio 4.44로 분기한다.
gradio>=5.0,<6.0; python_version >= "3.10"
gradio>=4.44,<5.0; python_version < "3.10"
# gradio 4.x 는 huggingface_hub 1.x 에서 제거된 심볼(HfFolder)을 임포트하고,
# pydantic 2.11+ 가 만드는 JSON 스키마(additionalProperties: bool)를 파싱하지 못한다.
huggingface_hub>=0.25,<1.0; python_version < "3.10"
pydantic>=2.0,<2.11; python_version < "3.10"

# --- 모델 / 영상 ---
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.9.0
lap>=0.5.12
imageio-ffmpeg>=0.5.1
